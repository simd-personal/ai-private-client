import { BuyerQuiz } from "@/components/quiz/buyer-quiz";

export const metadata = {
  title: "Private Client Property Brief | Private Client Property Desk",
};

export default function BuyerPage() {
  return <BuyerQuiz />;
}
